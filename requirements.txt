# WSC2026 Day3 — 파이썬 툴 의존성
#   설치: pip install -r requirements.txt
#   (나머지 표준 라이브러리(asyncio/subprocess/urllib/json/threading 등)는 설치 불필요)

# ── 핵심 (튜닝 / WAF) ──
aiohttp>=3.9          # turn.py : 부하 생성·측정 (필수)
boto3>=1.34           # update_waf.py : WAF 헤더 룰 조작 (헤더 룰 쓸 때만)
#   · scaler.py 는 표준 라이브러리만 사용 → 추가 설치 없음

# ── 모니터링 (선택) ──
flask>=3.0            # dashboard.py : 웹 대시보드
windows-curses>=2.3 ; sys_platform == "win32"   # podlog.py : 터미널 UI (Windows에서만 필요)
